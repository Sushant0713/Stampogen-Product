import { CustomerHistory } from '@/features/customer/CustomerHistory';

export const metadata = {
  title: 'History | Stampogen',
};

export default function CustomerHistoryPage() {
  return <CustomerHistory />;
}
